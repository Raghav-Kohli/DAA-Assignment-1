#include <iostream>
using namespace std;

int main() {
    int a[]={-2,-5,6,-2,-3,1,5,-6}, s=a[0], m=a[0];
    for(int i=1;i<8;i++)
        s = (s+a[i]>a[i]) ? s+a[i] : a[i],
        m = (m>s) ? m : s;
    cout << m;
}
